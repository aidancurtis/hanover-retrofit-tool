  # Hanover Retrofit Tool

  This is code for a retrofit suggestion calculator for Hanover, NH.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
